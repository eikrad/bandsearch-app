{
  "name": "@bandsearch/schemas",
  "version": "0.4.0",
  "private": true,
  "description": "Shared schema contracts package",
  "scripts": {
    "lint": "eslint \"src/**/*.ts\" \"test/**/*.ts\" --max-warnings=0 --no-error-on-unmatched-pattern",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "tsx --test"
  }
}
