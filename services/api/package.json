{
  "name": "@bandsearch/api",
  "version": "0.1.0",
  "private": true,
  "description": "Bandsearch Express API",
  "scripts": {
    "lint": "eslint \"src/**/*.js\" \"test/**/*.js\" --max-warnings=0 --no-error-on-unmatched-pattern",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test"
  },
  "dependencies": {
    "@langchain/google-genai": "^0.2.13",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "express-rate-limit": "^8.4.1",
    "helmet": "^8.1.0"
  }
}
