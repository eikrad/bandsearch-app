#!/usr/bin/env sh
# Prefer project venv so ruff/black work when global tools are not on PATH (e.g. Cursor sandboxes).
if [ -d "$(dirname "$0")/../.venv" ]; then
  export PATH="$(cd "$(dirname "$0")/.." && pwd)/.venv/bin:$PATH"
fi

npm run lint
npm run typecheck
npm test
